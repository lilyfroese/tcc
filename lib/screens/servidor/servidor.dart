import 'package:flutter/material.dart';

import '../servidor/components/body.dart';

class Servidor extends StatelessWidget {
  const Servidor({super.key});

  @override
  Widget build(BuildContext context) {
    return Body();
  }
}

